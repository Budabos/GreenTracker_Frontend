import {
    Card,
    CardContent,
    CardDescription,
    CardFooter,
    CardHeader,
    CardTitle,
} from "@/components/ui/card"

const FootprintDisplay = ({ resData }) => {
    return (
     
        <div>
            hello
       {resData.id}
      </div>
    )
}

export default FootprintDisplay